"""oape_agents: the demo's agents. The OpenAI Agents SDK owns the top-level package name
`agents`, so this package is `oape_agents`; CLAUDE.md's `agents/` layout maps 1:1 onto it
(common, sweeps, triage, drift, openai_estate)."""
